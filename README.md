# Line-Following-Robot-using-PID-control.
Arduino code for a Line Following Robot (LFR) using PID control. It reads data from an IR sensor array and adjusts motor speed (L298) in real time to follow the line smoothly and accurately. This repository contains only the main code used for controlling the robot.
